build:
  stage: build
  script:
    - make cppcheck
    - make cmake compile
  cache:
    paths:
      - build/debug/*
      - build/release/*
      - laslib/LASlib/src/*.o
      - laslib/LASzip/src/*.o

test:
  stage: test
  script:
    - make test
    - make memcheck
