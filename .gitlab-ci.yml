build:
  stage: build
  script:
    - make cmake compile
  artifacts:
    paths:
      - build/debug/*
      - build/release/*
  cache:
    paths:
      - laslib/LASlib/src/*.o
      - laslib/LASzip/src/*.o

test:
  stage: test
  script:
    - make test
